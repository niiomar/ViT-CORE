[tool.ruff]
line-length = 110
target-version = "py310"
extend-exclude = [".venv_test", "ViT-CORE.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.coverage.run]
source = ["."]
omit = ["tests/*", ".venv*/*"]
